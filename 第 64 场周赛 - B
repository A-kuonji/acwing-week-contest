#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int main()
{
    int n; cin >> n;

    while(n--) {
        string s; cin >> s;
        if(s[s.size() - 2] == 'p') cout << "FILIPINO" << endl;
        else if(s[s.size() - 2] == 's') cout << "JAPANESE" << endl;
        else cout << "KOREAN" << endl;
    }
    return 0;
}
